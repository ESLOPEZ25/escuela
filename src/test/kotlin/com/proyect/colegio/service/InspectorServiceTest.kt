package com.proyect.colegio.service

import com.google.gson.Gson
import com.proyect.colegio.service.InspectorServise
import org.apache.tomcat.jni.File
import org.junit.jupiter.api.Assertions
import org.junit.jupiter.api.Test
import org.mockito.InjectMocks
import org.mockito.Mock
import org.mockito.Mockito
import org.springframework.boot.test.context.SpringBootTest


@SpringBootTest
class InspectorServiceTest {
   /*@InjectMocks
    lateinit var inspectorServise: InspectorServise

    @Mock
    lateinit var  inspectorServise: InspectorServise
    val jsonString = File("./src/test/resuor/Inspector/crearInspector.json").readText()
    val inpectorMock = Gson().fromJson()


    @Test
    fun saveIsCorrect(){
        Mockito.`when`(inspectorServise.save(Mockito.any(Diet::class.java))).thenReturn(returnObject)
        val response = inspectorServise.save(newObject)
        Assertions.assertEquals(response.id, newObject.id)
        Assertions.assertEquals(response.description, newObject.description)


    }*/
}